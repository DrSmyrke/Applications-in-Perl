#!/bin/bash
dd if=$1/$3 of=$2
#user@host:~# dd if=mbr.bin of=/dev/sda
#Для того чтобы восстановить только код загрузчика, а именно первые 446 байт MBR нужно выполнить команду.

#user@host:~# dd if=mbr.bin of=/dev/sda bs=446 count=1
#Для восстановления только таблицы разделов необходимо выполнить следующую команду.

#user@host:~# dd if=mbr.bin of=/dev/sda bs=1 skip=446 seek=446 count=64
#Также иногда бывает необходимо восстановить таблицу разделов вместе с сигнатурой загрузчика для этого нужно выполнить предыдущую команду с добавлением двух байт сигнатуры загрузчика.

#user@host:~# dd if=mbr.bin of=/dev/sda bs=1 skip=446 seek=446 count=66
